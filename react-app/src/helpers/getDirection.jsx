export const getDirection = (lang) => (lang === 'eng' ? 'ltr' : 'rtl') //if lang is eng return ltr else return rtl
//next step is to go to app.js and create rtl cache
//then in index.html add direction in rtl and overflow x
